# SJZ260401

Static slide deck for GitHub Pages.

## Publish

1. Push `main` to GitHub.
2. In repository Settings -> Pages, set Source to `Deploy from a branch`.
3. Choose branch `main` and folder `/ (root)`.
4. Wait for the Pages build to finish.

Expected site URL:

`https://simbacd.github.io/SJZ260401/`
